//! Browser event conversion (module preserved for future use).
//!
//! Previously contained browser event to Hub action conversion functions.
//! These were removed in Phase 5 cleanup as BrowserClient now handles
//! event routing directly via its async task and hub channel subscription.
