#!/bin/bash
# Background dev server for tunnel preview
# This runs when an agent spawns with BOTSTER_TUNNEL_PORT set
#
# Available environment variables:
#   BOTSTER_TUNNEL_PORT - Port to run the server on
#   BOTSTER_WORKTREE_PATH - Path to the worktree
#
# Customize this file for your project (Rails, Node, Python, etc.)

bin/rails server -p "$BOTSTER_TUNNEL_PORT" -b 127.0.0.1
