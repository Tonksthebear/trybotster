service: trybotster
image: conig/trybotster

servers:
  web:
    - 5.78.156.108
  mcp:
    hosts:
      - 5.78.156.108
    cmd: bundle exec puma mcp.ru -b tcp://0.0.0.0:62770
    proxy:
      ssl: true
      host: mcp.trybotster.com
      app_port: 62770

proxy:
  ssl: true
  host: trybotster.com

registry:
  server: registry.brinkstermeister.com
  username:
    - KAMAL_REGISTRY_USERNAME
  password:
    - KAMAL_REGISTRY_PASSWORD

env:
  secret:
    - RAILS_MASTER_KEY
    - POSTGRES_PASSWORD
    - TURN_SECRET
    - GITHUB_APP_ID
    - GITHUB_APP_CLIENT_ID
    - GITHUB_APP_CLIENT_SECRET
    - GITHUB_WEBHOOK_SECRET
  clear:
    SOLID_QUEUE_IN_PUMA: true
    DB_HOST: 172.17.0.1
    DB_PORT: 5433
    HOST_URL: trybotster.com
    TURN_SERVER_URL: "turn:turn.trybotster.com:3478"
    GITHUB_APP_PRIVATE_KEY_PATH: /rails/config/github_app_private_key.pem

volumes:
  - "trybotster_storage:/rails/storage"
  - "/root/trybotster-github-app.pem:/rails/config/github_app_private_key.pem:ro"

asset_path: /rails/public/assets

builder:
  arch: amd64

aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

accessories:
  db:
    image: postgres:16
    host: 5.78.156.108
    port: "5433:5432"
    env:
      clear:
        POSTGRES_DB: trybotster_production
        POSTGRES_USER: trybotster
      secret:
        - POSTGRES_PASSWORD
    directories:
      - trybotster_pgdata:/var/lib/postgresql/data

  turn:
    image: coturn/coturn:latest
    host: 5.78.156.108
    options:
      publish:
        - "3478:3478"
        - "3478:3478/udp"
        - "49152-49252:49152-49252/udp"
    files:
      - config/turnserver.conf:/etc/turnserver.conf:ro
    env:
      secret:
        - TURN_SECRET
    cmd: -c /etc/turnserver.conf --static-auth-secret=$TURN_SECRET
